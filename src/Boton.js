// function Boton(props) {
//   return <button onClick={props.hagaAlgo}>componente boton</button>;
// }

const Boton = ({ hagaAlgo }) => (
  <button onClick={hagaAlgo}>componente boton</button>
);

export default Boton;
